# Call this workflow from other jobs to execute unit tests in the current context

name: Run unit tests (reusable)

on:
  workflow_call:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    # implementation at 'source/generic_make_go.mk'
    - name: Run tests
      run: make new-tests