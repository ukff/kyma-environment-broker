apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "kyma-env-broker.fullname" . }}-subaccount-sync-dashboard
  namespace: {{ .Release.Namespace }}
  labels:
    grafana_dashboard: "1"
    app: monitoring-grafana
data:
  {{ include "kyma-env-broker.fullname" . }}-subaccount-sync-dashboard.json: |-
{{ .Files.Get "files/subaccount-sync-dashboard.json" | indent 4 }}
